# Tools

Tools is a library that attempts to abstract a lot of commonly used operations, it also tries to provide exstensive error coverage to allow developers to understand exactly why errors occured and how to fix them, covering as many edge cases as possible

Tools aims to be a library that works the same regardless of operating system, meaning compiling for multiple operating systems can be done with ease