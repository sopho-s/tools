# Tools

Tools is a library that attempts to abstract a lot of commonly used operations on the linux system, it also tries to provide exstensive error coverage to allow developers to understand exactly why errors occured and how to fix them, covering as many edge cases as possible
